# Smart Oil Gauge CI/CD reference (inspected 2026-09-05)

The live `andrewtryder/ha-smart-oil-gauge` repository uses push/PR validation on `main` and `master`, a daily UTC schedule, and manual dispatch. Its jobs are `validate-hacs`, `validate-hassfest`, and `validate-tests`; the latter runs Python 3.14, Ruff check/format, and pytest with a 96% coverage floor. It pins HACS action `1ebf01c…`, checkout `3d3c42e…` (v7.0.1), setup-python `5fda3b…` (v7.0.0), upload-artifact `043fb4…` (v4.6.1), and Release Please `45996e…` (v5.0.0). Hassfest is pinned to `a7c616c…`.

Semantic title validation uses `pull_request_target` only for PR metadata, with no checkout, workflow permissions `{}`, job `pull-requests: read`, and step-security semantic PR action `75d2dd…` (v6.1.2). It accepts the standard listed Conventional Commit types and requires lowercase subjects.

Release Please runs on main pushes with `contents` and `pull-requests` write permissions, uses simple release type and an extra manifest-version JSON file. Dependabot updates Actions and pip dependencies weekly. Pre-commit runs whitespace/YAML/JSON/large-file checks plus Ruff 0.16.4 fix/format hooks.

Live repository settings: public, default `main`, squash enabled, merge/rebase disabled, branch deletion enabled, squash title `PR_TITLE`, squash message `PR_BODY`, and automatic merge disabled. Releases/tags use `vX.Y.Z` and are generated by `github-actions[bot]`. The reference had no rulesets. This token received HTTP 403 for branch-protection and Actions-permissions APIs, so their live values could not be read.
